Code for dissertation. Focus on main1.ipynb and main2.ipynb , as Small_Changes.ipynb is a smaller, near duplicated version
of the overall notebook, with maybe one or two parameters changed in one of the plots, included, just in case a result depends 
on this older version of the code which was run on my laptop before I realised I could swap to my CPU when using my PC, as the 
forbenius norm width code used to cause memory limits on my GPU.

mains are split up into the first experiment and second experiment. They should be run as one big notebook, but it exceeds
the max allowed in Git's upload so I split them into two (these contained pre-computed images, results etc hence why they exceed
the limit)
